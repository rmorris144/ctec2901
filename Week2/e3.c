#include <stdio.h>
#include <stdlib.h>

void bubblesort( int a[], int N int (*rel) (int,int))
{
	int i,j;
	for (i=N-1; i>0; i--)
		for(j=0; j<i; j++)
		if (!rel(a[j], a[j+1}))
			SWAP(a,j,j+1);
}

void swap(char) *x, char *y) {
	
	int tmp;
	tmp = *x; *x = *y; *y = tmp;
	
	return;
}
			
			int main ()
			{
    
                         printf("the result of a is: %i", x);
                         printf("the result of y is: %i", y);
				//int less_eq(char x, char y);
			//	int greater_eq(char x, char y);
				
				
				
			//	Ok2R
			//	NwyW


